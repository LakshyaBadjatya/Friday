# Secrets — never commit
.env
.env.*
!.env.example
*.key
/secrets/
data/secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.cache/

# uv
uv.lock.tmp

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Runtime artifacts
*.log
data/
audio_out/
chroma/

# Playwright MCP artifacts + stray screenshots (keep assets/)
.playwright-mcp/
/*.png

# Internal planning docs — kept locally, never published
docs/
FRIDAY-build-spec.md

# Agent / local working dirs — kept locally, never published
.claude/
seo-deliverables/

# Firebase web config (public key, but kept out of git per request)
web/firebase-config.js
