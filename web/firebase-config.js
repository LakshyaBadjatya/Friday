// Firebase web config for project 367861861540.
//
// Fill these in from the Firebase Console:
//   Project settings (gear icon) -> "Your apps" -> the Web app -> "SDK setup and
//   configuration" -> Config. Or let the Firebase CLI fill them via
//   `firebase apps:sdkconfig web`.
//
// A Firebase web config is meant to ship to the browser; it is NOT a secret —
// access is controlled by Firebase Auth + Firestore Security Rules, not by hiding
// this object.
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_API_KEY",
  authDomain: "REPLACE_PROJECT_ID.firebaseapp.com",
  projectId: "REPLACE_WITH_PROJECT_ID",
  storageBucket: "REPLACE_PROJECT_ID.appspot.com",
  messagingSenderId: "367861861540",
  appId: "REPLACE_WITH_APP_ID",
};
